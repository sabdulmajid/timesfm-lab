# Experiment log

This append-only index links committed run configurations to raw local logs and curated
aggregate artifacts. Failed and negative runs remain visible.

No experiments have completed yet.
